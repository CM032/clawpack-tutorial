{
    "name": "Clawpack",
    "image": "ghcr.io/cm032/images/python-clawpack:latest",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance",
                "ms-toolsai.jupyter",
                "wesbos.theme-cobalt2"
            ]
        }
    },
    "containerEnv": {
        "GEMINI_API_KEY": "${localEnv:GEMINI_API_KEY}"
    },
    "postCreateCommand": "mkdir -p ~/.gemini && [ -n \"$GEMINI_API_KEY\" ] && echo \"GEMINI_API_KEY=$GEMINI_API_KEY\" > ~/.gemini/.env && echo '✅ GEMINI_API_KEY saved to ~/.gemini/.env' || echo '⚠️  GEMINI_API_KEY not set — add it as a Codespaces secret'"
}